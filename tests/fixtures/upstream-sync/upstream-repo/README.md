This directory documents the upstream-sync test fixture space.

The integration tests create temporary git repositories at runtime instead of
committing a nested `.git` fixture into the main repository.
