.DS_Store
*.log
node_modules/

# Compiled binaries (root level only)
/gale-task
.codex/
todos/
.worktrees
.context/
.claude/worktrees/
__pycache__/
*.pyc

# Local-only scripts (not committed — contain upstream sync tooling)
.local/
.upstream-ref
.upstream-repo
.compound-upstream
.galeharness-cli/*.local.yaml

# Sync temp files
.compound-sync.diff
.compound-sync.raw.diff
.qoder/

# Local test scripts
test-gh-local
docs/async-progress/WINDOWS_COMPAT_SCAN_REPORT.md
# Local HKTMemory / GaleHarness runtime state
memory/_lifecycle/
memory/*.db
memory/layer_relationships.json
