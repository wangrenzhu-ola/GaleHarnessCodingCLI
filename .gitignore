.DS_Store
*.log
node_modules/
.codex/
todos/
.worktrees
.context/
.claude/worktrees/

# Local-only scripts (not committed — contain upstream sync tooling)
.local/
.upstream-ref
.galeharness-cli/*.local.yaml

# Sync temp files
.compound-sync.diff
